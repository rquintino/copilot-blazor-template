Primary instructions live in `AGENTS.md` at the repo root — read it first.

Available skills live in `.github/skills/<name>/SKILL.md`. Inspect each skill's frontmatter (`name`, `description`) and invoke when its trigger fires. The skill index with trigger conditions is in `AGENTS.md` → **Skills**.
